apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: uforetrygd-frontend-veileder
  namespace: pensjonselvbetjening
  labels:
    team: pensjonselvbetjening
spec:
  groups:
    - name: uforetrygd-frontend-veileder-alerts
      rules:
        - alert: Application is unavailable
          expr: kube_deployment_status_replicas_available{deployment="uforetrygd-frontend-veileder"} == 0
          for: 1m
          annotations:
            consequence: Uforetrygd veileder application is unavailable
            action: https://logs.adeo.no/app/r/s/tNK6s
          labels:
            namespace: pensjonselvbetjening # required
            severity: critical
        - alert: Failing deployment
          expr: kube_deployment_status_replicas_unavailable{deployment="uforetrygd-frontend-veileder"} > 1
          for: 5m
          annotations:
            consequence: Uforetrygd veileder failing deployment
            action: https://logs.adeo.no/app/r/s/tNK6s
          labels:
            namespace: pensjonselvbetjening # required
            severity: critical
