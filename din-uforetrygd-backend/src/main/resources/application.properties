spring.application.name=uforetrygd-backend
spring.main.web-application-type=servlet
server.error.include-message=always
server.max-http-request-header-size=16KB
spring.main.banner-mode=off

management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true
management.endpoints.web.exposure.include=prometheus, health, info, gateway

oauth2.azureAd.clientId=${AZURE_APP_CLIENT_ID}
oauth2.azureAd.clientSecret=${AZURE_APP_CLIENT_SECRET}
oauth2.azureAd.issuer=${AZURE_OPENID_CONFIG_ISSUER}
oauth2.azureAd.jsonWebKeyUri=${AZURE_OPENID_CONFIG_JWKS_URI}
oauth2.azureAd.tokenEndpoint=${AZURE_OPENID_CONFIG_TOKEN_ENDPOINT}

fortrolig-tilgang.group.id=${FORTROLIG_TILGANG_GROUP_ID}
strengt-fortrolig-tilgang.group.id=${STRENGT_FORTROLIG_TILGANG_GROUP_ID}
skjermet-tilgang.group.id=${SKJERMET_TILGANG_GROUP_ID}
brukerhjelpa.group.id=${BRUKERHJELPA_GROUP_ID}
okonomi.group.id=${OKONOMI_GROUP_ID}
saksbehandler.group.id=${SAKSBEHANDLER_GROUP_ID}
veileder.group.id=${VEILEDER_GROUP_ID}
klagebehandler.group.id=${KLAGEBEHANDLER_GROUP_ID}

oauth2.tokenX.clientId=${TOKEN_X_CLIENT_ID}
oauth2.tokenX.issuer=${TOKEN_X_ISSUER}
oauth2.tokenX.jsonWebKeyUri=${TOKEN_X_JWKS_URI}
oauth2.tokenX.tokenEndpoint=${TOKEN_X_TOKEN_ENDPOINT}
oauth2.tokenX.privateJwk=${TOKEN_X_PRIVATE_JWK}

fullmakt.endpoint.url=${FULLMAKT_URL}
fullmakt.scope=${FULLMAKT_SCOPE}
fullmakt.audience=${FULLMAKT_AUDIENCE}

parallellesannheter.endpoint.url=${PARALLELLE_SANNHETER_URL}

pdl.endpoint.url=${PDL_URL}
pdl.scope=${PDL_SCOPE}
pdl.audience=${PDL_AUDIENCE}

pen.endpoint.url=${PEN_URL}
pen.scope=${PEN_SCOPE}
pen.audience=${PEN_AUDIENCE}

persondata.endpoint.url=${PERSONDATA_URL}
persondata.scope=${PERSONDATA_SCOPE}
persondata.audience=${PERSONDATA_AUDIENCE}

forstegangssoknad.endpoint.url=${FORSTEGANGSSOKNAD_ENDPOINT_URL}
forstegangssoknad.scope=${FORSTEGANGSSOKNAD_SCOPE}
forstegangssoknad.audience=${FORSTEGANGSSOKNAD_AUDIENCE}

skjerming.endpoint.url=${SKJERMING_URL}
skjerming.scope=${SKJERMING_SCOPE}